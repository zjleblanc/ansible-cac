# ansible-playbook pb_aap_config.yml -e "domains=hub" -e "skip_common=true" --tags roles
---
ah_roles_hub:
  - name: coach
    description: manage the astros (aka users)
    perms:
      - add_user
      - change_user
      - delete_user
      - view_user
